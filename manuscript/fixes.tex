\usepackage{annotate-equations}

% Use Helvetica (sans-serif) as the main text font; math stays in
% Computer Modern via the default LaTeX math fonts.
% Quarto renders this manuscript with LuaLaTeX, so use fontspec's
% \setmainfont rather than the pdfLaTeX-era \usepackage{helvet}+
% \renewcommand\familydefault. TeX Gyre Heros is the open-source
% Helvetica clone that ships with TeX Live (so it's available even
% if the system has no Helvetica).
\usepackage{iftex}
\ifLuaTeX
  \usepackage{fontspec}
  \setmainfont{TeX Gyre Heros}
\else
  \usepackage[scaled]{helvet}
  \renewcommand\familydefault{\sfdefault}
\fi


% \usepackage[singlelinecheck=false]{caption}
\usepackage{subcaption}

\makeatletter
\renewcommand{\fnum@figure}{\figurename~\thefigure}
\makeatother

% \DeclareCaptionLabelSeparator{mybar}{\;|\;}
%
% \captionsetup[figure]{
%   labelsep=mybar,
%   font=small,
%   labelfont=bf,
%   textfont=normalfont
% }
%
% \captionsetup[table]{
%   labelsep=mybar,
%   font=small,
%   labelfont=bf,
%   textfont=normalfont
% }


% Optional: no labels for subfigures
\captionsetup[subfigure]{labelformat=empty}


% Suppress the automatic author row from \maketitle so the explicit
% Authors / Affiliations / Corresponding-author block that follows the
% title in the body is the sole rendering of author information on
% the title page. (Genome Medicine round-2 revision.)
\makeatletter
\def\@maketitle{%
  \newpage\null\vskip 2em%
  \begin{center}%
    {\LARGE \@title \par}%
  \end{center}\par\vskip 1.5em%
}
\makeatother



